const windowWidth = 800;
const windowHeight = 600;

export const sceneWidth = Math.round(windowWidth * 0.95);
export const sceneHeight = Math.round(windowHeight * 0.95);

export const verticalMidline = sceneWidth / 2;
export const horizontalMidline = sceneHeight / 2;
export const leftQuarterLine = sceneWidth / 4;
export const rightQuarterLine = sceneWidth - (sceneWidth / 4);
